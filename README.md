# pointOfInterestApi
implement an API (json) that receives the startingPoint and the EndPoint and returns a list of highlights; develop an endpoint that receives any lat long and returns the closest highlight.
